namespace TerraBend.Custom.Enums {
    /// <summary>
    /// Slightly specific type of enum that contains all
    /// of the Jing Types, and a special "Balanced" type
    /// that is only used for a True Jing Purge.
    /// </summary>
    public enum JingType {
        Positive,
        Neutral,
        Negative,
        Unaligned,
        Balanced
    }
}